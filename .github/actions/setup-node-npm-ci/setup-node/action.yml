name: Setup Node
description: Node 22 + npm ci with cache

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "22"
        cache: npm

    - shell: bash
      run: npm ci
